{
  "name": "olve-trains-ui",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outdir=public/dist --format=esm",
    "dev": "npm-run-all --parallel build:watch serve",
    "build:watch": "esbuild src/index.ts --bundle --outdir=public/dist --format=esm --watch",
    "serve": "lite-server"
  },
  "devDependencies": {
    "lite-server": "^2.6.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.2.2",
    "esbuild": "^0.25.5"
  }
}
