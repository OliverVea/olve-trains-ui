{
  "name": "olve-trains-ui",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "dev": "npm-run-all --parallel build:watch serve",
    "build:watch": "tsc -w",
    "serve": "lite-server"
  },
  "devDependencies": {
    "lite-server": "^2.6.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.2.2"
  }
}
